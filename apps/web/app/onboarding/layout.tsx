import type { Metadata } from 'next';

export { OnboardingLayout as default } from '@/app/layouts';

export const metadata: Metadata = {
  title: 'Onboarding',
  description: 'where users can sign up with their github account',
};
